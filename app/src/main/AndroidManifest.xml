<?xml version="1.0" encoding="utf-8"?>

<!--
  ~ Copyright (c) 2016 Thomas Christensen <christensenthomas@gmail.com>
  ~
  ~ All right reserved.
  -->
<manifest
    package="thomasc.loananalyzer"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.LoansActivity"
                  android:label="@string/loan_analyzer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.EditorActivity"
                  android:label="@string/editor">
        </activity>

        <activity android:name=".activities.ScheduleActivity"
                  android:label="@string/schedule">
        </activity>

        <activity android:name=".activities.HelpActivity"
                  android:label="@string/help">
        </activity>

    </application>

</manifest>
